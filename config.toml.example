[api]
url   = "http://localhost:11434/v1"
model = "your-model-name"
# api_key_header = "api-key"  # 省略時は Authorization: Bearer (標準動作)
# context_window = 32768      # モデルのコンテキスト長(トークン)。設定するとコンテキスト圧縮が有効になる

[storage]
save_dir = "./trees"

[system]
prompt = ""
